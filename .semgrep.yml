# Semgrep registry packs are passed via --config in the pipeline:
# --config p/owasp-top-ten
# --config p/secrets
# --config p/python
# --config p/docker
#
# This file is kept for any future custom rules.
# Example custom rule:
#
# rules:
#   - id: no-eval
#     pattern: eval(...)
#     message: Avoid using eval()
#     severity: ERROR
#     languages: [python]
